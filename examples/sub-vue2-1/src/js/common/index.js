import common from './src/common'
import item from './src/item'
import order from './src/order'
import environment from './src/environment'

export default { ...common, ...item, ...order, ...environment }
